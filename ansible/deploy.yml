- name: Deploy App using Ansible
  hosts: local
  become: yes

  tasks:

    - name: Copy application files
      copy:
        src: "{{ playbook_dir }}/../app/"
        dest: /var/www/html/

    - name: Restart Nginx
      service:
        name: nginx
        state: restarted
